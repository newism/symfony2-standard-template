{% extends 'ClubEvoApiBundle::layout.html.twig' %}

{% block content -%}
    <h1>Distributor Employee</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ entity.role }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <a href="{{ path('distributor_employee_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </div>
    <div>
        <a href="{{ path('distributor_employee_browse') }}">
            Back to the list
        </a>
    </div>
{% endblock %}
