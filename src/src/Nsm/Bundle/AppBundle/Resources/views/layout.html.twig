{# A layout router / proxy which first checks for an ajax request then a _layout query var #}

{% set layout = app.request.xmlHttpRequest ? "page" : "app" %}
{% set layout = app.request.get('_layout') ?: layout %}

{% extends "NsmAppBundle::layout-" ~ layout ~ ".html.twig" %}
