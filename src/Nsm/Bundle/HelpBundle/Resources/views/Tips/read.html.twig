{% extends 'NsmAppBundle::layout.html.twig' %}

{% block content -%}

    <h1>Tip</h1>

    <ul class="record_actions">
        <li>
            <a href="{{ path('tip_browse') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('tip_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>
            <a href="{{ path('tip_destroy', { 'id': entity.id }) }}">
                Destroy
            </a>
        </li>
    </ul>

    {{ dump(entity) }}

{% endblock %}
