            <tr>
                <td><a href="{{ path('activity_read', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title is defined ? entity.title }}</td>
                <td>{{ entity.description is defined ? entity.description }}</td>
                <td>{{ entity.createdAt is defined ? entity.createdAt|date('r') }}</td>
                <td>{{ entity.updatedAt is defined ? entity.updatedAt|date('r') }}</td>
                <td>{{ entity.deletedAt is defined ? entity.deletedAt|date('r') }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('activity_read', { 'id': entity.id }) }}">read</a>
                        </li>
                        <li>
                            <a href="{{ path('activity_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                        <li>
                            <a href="{{ path('activity_destroy', { 'id': entity.id }) }}">destroy</a>
                        </li>
                    </ul>
                </td>
            </tr>
