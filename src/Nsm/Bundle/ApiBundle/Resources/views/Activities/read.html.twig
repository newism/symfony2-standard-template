{% extends 'NsmApiBundle::layout.html.twig' %}

{% block body -%}
    <h1>Activity</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('activities_browse') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('activities_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
            <li>
                <a href="{{ path('activities_destroy', { 'id': entity.id }) }}">
                    Destroy
                </a>
            </li></ul>
{% endblock %}
