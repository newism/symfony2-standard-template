{% extends 'NsmAppBundle::layout.html.twig' %}

{% block content -%}
    <h1>Feature destroy</h1>

    <form action="{{ path('feature_delete', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
        <input type="hidden" name="_method" value="DELETE" />
        {{ form_widget(form) }}
        <p>
            <button type="submit">Destroy</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('feature_browse') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
