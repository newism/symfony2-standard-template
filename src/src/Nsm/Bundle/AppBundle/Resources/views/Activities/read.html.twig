{% extends 'NsmAppBundle::layout.html.twig' %}

{% block content -%}

    <h1>Activity</h1>

    <ul class="record_actions">
        <li>
            <a href="{{ path('activity_browse') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('activity_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>
            <a href="{{ path('activity_destroy', { 'id': entity.id }) }}">
                Destroy
            </a>
        </li>
    </ul>

    {{ dump(entity) }}

{% endblock %}
