{% extends 'ClubEvoApiBundle::layout.html.twig' %}

{% block content -%}
    <h1>Roadside Assist Employee destroy</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <p>
            <button type="submit">Destroy</button>
        </p>
    {{ form_end(form) }}

    <div>
        <a href="{{ path('roadside_assist_employee_browse') }}">
            Back to the list
        </a>
    </div>
{% endblock %}
